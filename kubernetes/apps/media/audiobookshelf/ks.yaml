---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: audiobookshelf
spec:
  interval: 1h
  path: ./kubernetes/apps/media/audiobookshelf/app
  # The library volume rides csi-driver-smb — make sure it's installed first.
  dependsOn:
    - name: csi-driver-smb
      namespace: storage
  postBuild:
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: media
  wait: false
