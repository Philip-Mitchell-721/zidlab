---
apiVersion: v1
kind: Namespace
metadata:
  name: observability
  labels:
    # node-exporter needs hostNetwork/hostPID/hostPath — blocked under baseline PodSecurity
    pod-security.kubernetes.io/enforce: privileged
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
