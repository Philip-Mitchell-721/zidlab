---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: karakeep
spec:
  chartRef:
    kind: OCIRepository
    name: karakeep
  interval: 1h
  values:
    controllers:
      # --- Web UI + API + the crawl/index workers ---
      karakeep:
        # single writer to the RWO /data volume (SQLite + archived assets)
        strategy: Recreate
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/karakeep-app/karakeep
              tag: 0.33.1
            env:
              TZ: America/New_York
              DATA_DIR: /data
              # used to build auth callback URLs — same hostname LAN or public,
              # so this survives the envoy-external flip
              NEXTAUTH_URL: https://karakeep.${SECRET_DOMAIN}
              MEILI_ADDR: http://karakeep-meilisearch:7700
              BROWSER_WEB_URL: http://karakeep-chrome:9222
              DISABLE_NEW_RELEASE_CHECK: "true"
              # captures the whole page instead of just the 1440x900 viewport. only
              # applies to newly-crawled bookmarks, and costs ~1-3MB per page vs
              # ~200KB — the /data PVC is 20Gi with <1MB used, so there is room
              CRAWLER_FULL_PAGE_SCREENSHOT: "true"
              # blocks OIDC provisioning too, and there is no admin create-user
              # path — adding a person means flipping this to "false", having
              # them log in, and flipping it back
              DISABLE_SIGNUPS: "true"
              DISABLE_PASSWORD_AUTH: "true"
              # only honoured once password auth is off
              OAUTH_AUTO_REDIRECT: "true"
              OAUTH_PROVIDER_NAME: Authentik
              # slug-scoped, unlike donetick's endpoints
              OAUTH_WELLKNOWN_URL: https://auth.${SECRET_DOMAIN}/application/o/karakeep/.well-known/openid-configuration
            envFrom:
              - secretRef:
                  name: karakeep-secret
            probes:
              liveness:
                enabled: true
                spec:
                  # s6 runs DB migrations before the server binds 3000; the default
                  # 0s delay would restart-loop a slow first boot
                  initialDelaySeconds: 60
              readiness:
                enabled: true
            resources:
              requests:
                cpu: 100m
                memory: 512Mi
              limits:
                memory: 2Gi
      # --- Headless Chrome the crawler drives over the devtools port. Stateless;
      # it is also the RAM hog of the three. ---
      chrome:
        containers:
          app:
            image:
              # upstream retired the zenika image (the :124 tag never existed) — this
              # is karakeep's own build of chromedp/headless-shell, the only browser
              # they now test against
              repository: ghcr.io/karakeep-app/karakeep-chrome
              tag: 151.0.7922.47-r1
            # entrypoint run.sh already passes --no-sandbox and
            # --remote-debugging-address=0.0.0.0, and binds chrome on 9223 with socat
            # fronting 9222 — passing --remote-debugging-port here would collide with
            # that listener. what is left is upstream's compose flag list verbatim,
            # incl. in-memory /dev/shm which containers cap at 64Mi
            args:
              - --disable-gpu
              - --disable-dev-shm-usage
              - --hide-scrollbars
              - --disable-blink-features=AutomationControlled
              - --window-size=1440,900
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              requests:
                cpu: 50m
                memory: 256Mi
              limits:
                memory: 1Gi
      # --- Meilisearch: full-text index. Rebuildable from the DB, so the volume is
      # replaceable — but it must not run two writers either. ---
      meilisearch:
        strategy: Recreate
        containers:
          app:
            image:
              repository: docker.io/getmeili/meilisearch
              tag: v1.41.0
            env:
              TZ: America/New_York
              MEILI_NO_ANALYTICS: "true"
            envFrom:
              # shares the secret with the web container — MEILI_MASTER_KEY has to
              # match on both ends or every search 403s
              - secretRef:
                  name: karakeep-secret
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              requests:
                cpu: 50m
                memory: 256Mi
              limits:
                memory: 1Gi
    service:
      # keys become `karakeep-app` / `karakeep-chrome` / `karakeep-meilisearch`
      # — the last two are the DNS names the web container is pointed at above
      app:
        controller: karakeep
        ports:
          http:
            port: &port 3000
      chrome:
        controller: chrome
        ports:
          http:
            port: 9222
      meilisearch:
        controller: meilisearch
        ports:
          http:
            port: 7700
    persistence:
      data:
        existingClaim: karakeep-data
        advancedMounts:
          karakeep:
            app:
              - path: /data
      meilisearch:
        existingClaim: karakeep-meilisearch
        advancedMounts:
          meilisearch:
            app:
              - path: /meili_data
    route:
      app:
        # Public via the Cloudflare tunnel; external-dns publishes karakeep.<domain>.
        # Safe to expose because OIDC is the only login (password auth disabled above).
        hostnames: ["karakeep.${SECRET_DOMAIN}"]
        parentRefs:
          - name: envoy-external
            namespace: network
            sectionName: https
        rules:
          - backendRefs:
              - identifier: app
                port: *port
