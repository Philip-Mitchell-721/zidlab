---
apiVersion: v1
kind: Namespace
metadata:
  name: storage
  labels:
    # the CSI node plugin mounts filesystems on the host — needs privileged
    pod-security.kubernetes.io/enforce: privileged
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
